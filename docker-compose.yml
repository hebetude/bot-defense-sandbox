networks:
  defense-net:
    driver: bridge

volumes:
  loki-data:
  grafana-data:
  wordpress-db-data:
  wordpress-data:

include:
  - compose.traefik.yml
  - compose.wordpress.yml
  # - compose.wiki.yml